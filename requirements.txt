# processing/ -- pure geospatial computation, no web dependencies (PLAN.md 10)
rasterio>=1.5.0
numpy>=2.0

# optional: full COG validation. Without it cog.validate_cog falls back to a
# structural check (tiled / overviews / nodata).
rio-cogeo>=5.0

# tests
pytest>=8.0

# catalogue/ -- geometry for AOI/footprint intersection
shapely>=2.0

# probes/benchmark_pipeline.py only
psutil>=5.9

# backend/ -- API layer
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27          # required by fastapi.testclient
